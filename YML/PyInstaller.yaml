# PyInstaller Configuration File
block_cipher = None

a = Analysis(['./Src/Evolve3D.py'],  # Updated to point to your main script
             pathex=['./Src'],  # Updated to the src directory path
             binaries=[],
             datas=[],
             hiddenimports=['numpy', 'sklearn', 'tkinter', 'threading', 'mayavi', 'copy', 'os', 'random', 'time'],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=None,
             noarchive=False)

pyz = PYZ(a.pure, a.zipped_data,
          cipher=block_cipher)

exe = EXE(pyz,
          a.scripts,
          [],
          exclude_binaries=True,
          name='GENevolver3D',  # Set the application name
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=False,  # False for GUI application
          icon=None,
          disable_windowed_traceback=False,
          target_arch=None,
          codesign_identity=None,
          entitlements_file=None)

# GUI options
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               upx_exclude=[],
               name='GENevolver3D')
